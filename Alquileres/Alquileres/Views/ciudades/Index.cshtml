@model IEnumerable<Alquileres.Models.ciudad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Provincias.Detalle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Detalle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Provincias.Detalle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Detalle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CiudadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CiudadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CiudadId })
        </td>
    </tr>
}

</table>
